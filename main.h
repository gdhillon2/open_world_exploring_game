#pragma once
#include <SDL.h>

// desired FPS
const Uint32 FPS = 60;
const Uint32 frameTime = 1000 / FPS;
const int animation_frame_length = 100; // length of each frame in ms

// window dimensions
const int window_height = 880;
const int window_width = 1470;
